This file is unstaged, it is not in the git index, and is not committed anywhere in the git repo.

Basically, this file isn't referenced anywhere in the .git directory, though git.git will report it's unloved existence when `git status` is run.
